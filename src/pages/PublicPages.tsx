import { useMemo, useState } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { motion } from 'framer-motion';
import { ArrowDown, ArrowRight, CalendarDays, Check, ChevronRight, Clock3, LockKeyhole, MapPin, ShieldCheck, Ticket, Users, Wifi } from 'lucide-react';
import { useEvent, useEvents } from '@/hooks/useEvents';
import { EventCard, EventMeta, EmptyState, LoadingState, SectionHeading, TrustStrip } from '@/components/ui';
import { formatCurrency, formatDate, formatTime } from '@/lib/utils/format';
import { useAuth } from '@/hooks/useAuth';

const heroImage = 'https://images.pexels.com/photos/14646741/pexels-photo-14646741.jpeg?auto=compress&cs=tinysrgb&w=2200&h=1400&fit=crop';

export function HomePage() {
  const { data: events = [], isLoading } = useEvents();
  const featured = events.find((event) => event.is_featured) || events[0];
  const upcoming = events.filter((event) => event.status === 'active').slice(0, 3);
  return <>
    <section className="relative flex min-h-[720px] items-end overflow-hidden bg-navy-950 pb-14 pt-32 md:min-h-[790px] md:pb-20"><img src={heroImage} alt="Elegant event hall prepared for a celebration" className="absolute inset-0 h-full w-full object-cover opacity-70" /><div className="absolute inset-0 hero-overlay" /><div className="absolute inset-0 bg-gradient-to-t from-navy-950/70 via-transparent to-navy-950/15" /><div className="relative mx-auto w-full max-w-7xl px-6 md:px-10"><motion.div initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .7 }} className="max-w-2xl text-white"><p className="section-label text-gold-200">PPSU Events 2026</p><h1 className="mt-5 text-5xl leading-[1.02] md:text-8xl">Experience More.<br /><span className="text-gold-200">Connect More.</span><br />Celebrate More.</h1><p className="mt-7 max-w-lg text-base leading-7 text-white/70 md:text-lg">Discover unforgettable university experiences, from elegant celebrations to high-energy student events.</p><div className="mt-9 flex flex-wrap gap-3"><Link to="/events" className="btn-primary">Explore events <ArrowRight size={17} /></Link><Link to={featured ? `/events/${featured.slug}` : '/events'} className="btn-outline">Book your experience</Link></div></motion.div><div className="mt-16 grid max-w-4xl grid-cols-2 gap-5 border-t border-white/20 pt-5 text-white md:grid-cols-4"><div><p className="text-xs uppercase tracking-[.2em] text-gold-200">Next experience</p><p className="mt-2 font-display text-2xl">{featured ? new Date(featured.event_date).toLocaleDateString('en-US', { day: '2-digit', month: 'short' }).toUpperCase() : '19 DEC'}</p></div><div><p className="text-xs uppercase tracking-[.2em] text-white/45">Featured event</p><p className="mt-2 text-sm font-semibold">{featured?.title || 'Coming soon'}</p></div><div><p className="text-xs uppercase tracking-[.2em] text-white/45">Venue</p><p className="mt-2 text-sm font-semibold">{featured?.venue.split(',')[0] || 'PPSU Campus'}</p></div><div><p className="text-xs uppercase tracking-[.2em] text-white/45">From</p><p className="mt-2 font-display text-2xl text-gold-200">{featured ? formatCurrency(featured.ticket_price) : '₹499'}</p></div></div></div><div className="absolute bottom-7 right-6 hidden items-center gap-3 text-xs uppercase tracking-widest text-white/45 md:flex"><ArrowDown size={15} /> Scroll to discover</div></section>
    <section className="px-6 py-24 md:px-10 md:py-32"><div className="mx-auto max-w-7xl">{featured ? <><div className="mb-10 flex items-end justify-between gap-6"><SectionHeading eyebrow="The highlight" title="Make this moment yours." text="A carefully curated calendar of experiences designed to bring the PPSU community together." /><Link className="hidden items-center gap-2 text-sm font-bold text-navy-900 hover:text-gold-600 sm:flex" to={`/events/${featured.slug}`}>View featured event <ArrowRight size={16} /></Link></div><EventCard event={featured} featured /></> : isLoading ? <LoadingState /> : <EmptyState title="The stage is being set" text="New PPSU experiences are coming soon." />}</div></section>
    <section className="bg-white px-6 py-24 md:px-10 md:py-32"><div className="mx-auto max-w-7xl"><div className="flex items-end justify-between gap-6"><SectionHeading eyebrow="Your calendar" title="Upcoming events" text="From intimate gatherings to campus-wide celebrations, find the experience that feels like yours." /><Link className="hidden items-center gap-2 text-sm font-bold text-navy-900 hover:text-gold-600 sm:flex" to="/events">View all events <ArrowRight size={16} /></Link></div>{upcoming.length > 0 ? <div className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{upcoming.map((event) => <EventCard key={event.id} event={event} />)}</div> : <div className="mt-12"><EmptyState title="More moments are on their way" text="Keep an eye on this space for the next PPSU experience." /></div>}</div></section>
    <WhySection /><ProcessSection /><section className="bg-navy-950 px-6 py-24 text-white md:px-10 md:py-32"><div className="mx-auto grid max-w-7xl items-center gap-12 lg:grid-cols-[1fr_auto]"><SectionHeading light eyebrow="Your next chapter" title="Your next unforgettable night starts here." text="Bring your people. Bring your energy. We will take care of the moment." /><Link className="btn-primary whitespace-nowrap" to="/events">Explore upcoming events <ArrowRight size={17} /></Link></div></section>
  </>;
}

function WhySection() { const items = [{ icon: SparkIcon, title: 'Memorable experiences', text: 'Thoughtfully produced events that stay with you long after the last song.' }, { icon: Users, title: 'Student community', text: 'Meet, connect, and celebrate with the people who make PPSU feel like home.' }, { icon: Ticket, title: 'Seamless booking', text: 'A clear, trusted booking journey from the first click to your digital ticket.' }, { icon: ShieldCheck, title: 'Verified & secure', text: 'PPSU students, admin-reviewed payments, and dependable entry at every event.' }]; return <section className="px-6 py-24 md:px-10 md:py-32"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="Why PPSU Events" title="More than an event." text="It is the feeling of belonging to something bigger, made tangible in one unforgettable night." /><div className="mt-16 grid gap-10 border-t border-navy-950/10 pt-10 sm:grid-cols-2 lg:grid-cols-4">{items.map(({ icon: Icon, title, text }, index) => <motion.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .08 }} key={title}><Icon className="text-gold-500" size={26} /><h3 className="mt-5 font-display text-2xl text-navy-950">{title}</h3><p className="mt-3 text-sm leading-6 text-muted">{text}</p></motion.div>)}</div></div></section>; }
function SparkIcon(props: { size?: number; className?: string }) { return <SparklesIcon {...props} />; }
function SparklesIcon({ size = 24, className }: { size?: number; className?: string }) { return <Wifi size={size} className={className} />; }
function ProcessSection() { const steps = ['Discover', 'Choose', 'Book', 'Pay', 'Experience']; return <section className="bg-ivory px-6 py-24 md:px-10 md:py-32"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="The PPSU way" title="From discovery to memory." text="A simple journey built around the experience, not the administration." /><div className="mt-16 grid gap-4 md:grid-cols-5">{steps.map((step, index) => <div key={step} className="relative border-l border-gold-400/50 py-2 pl-5 md:border-l-0 md:border-t md:pl-0 md:pt-6"><span className="text-xs font-bold tracking-widest text-gold-500">0{index + 1}</span><h3 className="mt-3 font-display text-3xl text-navy-950">{step}</h3>{index < steps.length - 1 && <ChevronRight className="absolute right-2 top-6 hidden text-gold-400 md:block" size={18} />}</div>)}</div><div className="mt-14"><TrustStrip /></div></div></section>; }

export function EventsPage() { const { data: events = [], isLoading, error } = useEvents(); const [filter, setFilter] = useState('upcoming'); const filtered = useMemo(() => events.filter((event) => filter === 'all' || (filter === 'featured' && event.is_featured) || (filter === 'sold-out' && event.status === 'sold_out') || (filter === 'upcoming' && ['active', 'sold_out'].includes(event.status))), [events, filter]); return <div className="pt-28"><section className="bg-navy-950 px-6 py-20 text-white md:px-10 md:py-28"><div className="mx-auto max-w-7xl"><p className="section-label text-gold-200">The PPSU calendar</p><h1 className="mt-5 max-w-3xl text-5xl md:text-7xl">Discover what’s happening.</h1><p className="mt-6 max-w-xl text-base leading-7 text-white/60">Find your next reason to dress up, show up, and make it count.</p></div></section><section className="px-6 py-16 md:px-10 md:py-24"><div className="mx-auto max-w-7xl"><div className="scrollbar-hide flex gap-2 overflow-auto border-b border-navy-950/10 pb-4">{[['upcoming', 'Upcoming'], ['featured', 'Featured'], ['all', 'All events'], ['sold-out', 'Sold out']].map(([value, label]) => <button key={value} onClick={() => setFilter(value)} className={`whitespace-nowrap rounded-full px-5 py-2.5 text-sm font-semibold transition ${filter === value ? 'bg-navy-950 text-white' : 'text-muted hover:bg-navy-950/5'}`}>{label}</button>)}</div>{error ? <p className="py-16 text-center text-red-600">Unable to load events. Please try again.</p> : isLoading ? <LoadingState /> : filtered.length === 0 ? <div className="mt-10"><EmptyState title="No events in this collection" text="Try another filter or check back soon for new experiences." /></div> : <div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{filtered.map((event) => <EventCard key={event.id} event={event} />)}</div>}</div></section></div>; }

export function EventDetailPage() { const { slug } = useParams(); const { data: event, isLoading, error } = useEvent(slug); const { user } = useAuth(); const navigate = useNavigate(); if (isLoading) return <div className="pt-28"><LoadingState /></div>; if (error || !event) return <div className="pt-40"><EmptyState title="Event not found" text="This experience may have moved or is no longer available." action={<Link to="/events" className="btn-primary">Back to events</Link>} /></div>; const remaining = Math.max(event.capacity - event.tickets_sold, 0); const book = () => navigate(user ? `/events/${event.slug}/book` : `/sign-in?redirect=/events/${event.slug}/book`); return <div className="pt-20"><section className="relative min-h-[560px] overflow-hidden bg-navy-950 text-white"><img src={event.banner_url || heroImage} alt={event.title} className="absolute inset-0 h-full w-full object-cover opacity-65" /><div className="absolute inset-0 bg-gradient-to-r from-navy-950 via-navy-950/70 to-transparent" /><div className="relative mx-auto flex min-h-[560px] max-w-7xl items-end px-6 pb-16 md:px-10 md:pb-24"><div className="max-w-3xl"><p className="section-label text-gold-200">PPSU Events 2026</p><h1 className="mt-5 text-5xl md:text-8xl">{event.title}</h1><div className="mt-7 flex flex-wrap gap-5 text-sm text-white/75"><span className="flex items-center gap-2"><CalendarDays size={16} className="text-gold-200" />{formatDate(event.event_date)}</span><span className="flex items-center gap-2"><Clock3 size={16} className="text-gold-200" />{formatTime(event.start_time)} – {formatTime(event.end_time)}</span><span className="flex items-center gap-2"><MapPin size={16} className="text-gold-200" />{event.venue}</span></div></div></div></section><section className="px-6 py-16 md:px-10 md:py-24"><div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-[1fr_390px]"><div><p className="section-label">The story</p><p className="mt-5 max-w-2xl whitespace-pre-line text-lg leading-8 text-navy-900/75">{event.description}</p><div className="mt-12"><EventMeta event={event} /></div><div className="mt-8 rounded-2xl border border-gold-400/30 bg-gold-50 p-5"><div className="flex items-center justify-between text-sm"><span className="font-semibold text-navy-950">{event.tickets_sold} of {event.capacity} seats reserved</span><span className="font-bold text-gold-600">{remaining} left</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-white"><div className="h-full rounded-full bg-gold-400 transition-all" style={{ width: `${Math.min((event.tickets_sold / event.capacity) * 100, 100)}%` }} /></div></div></div><aside className="h-fit rounded-[24px] bg-navy-950 p-7 text-white shadow-2xl lg:sticky lg:top-28"><p className="text-xs uppercase tracking-[.2em] text-gold-200">Reserve your place</p><p className="mt-5 font-display text-5xl">{formatCurrency(event.ticket_price)}</p><p className="mt-1 text-sm text-white/50">per attendee</p><div className="my-7 border-t border-white/10" /><div className="space-y-3 text-sm text-white/70"><p className="flex items-center justify-between"><span>Capacity</span><strong className="text-white">{event.capacity}</strong></p><p className="flex items-center justify-between"><span>Available</span><strong className="text-gold-200">{remaining}</strong></p><p className="flex items-center justify-between"><span>Payment</span><strong className="text-white">Manual UPI</strong></p></div><button onClick={book} disabled={event.status === 'sold_out'} className="btn-primary mt-8 w-full disabled:bg-white/15 disabled:text-white/40">{event.status === 'sold_out' ? 'Join waitlist' : 'Book tickets'} <ArrowRight size={17} /></button><p className="mt-4 text-center text-xs text-white/40">Admin-approved payment. Digital ticket on confirmation.</p></aside></div></section></div>; }

export function AboutPage() { return <div className="pt-28"><section className="px-6 py-20 md:px-10 md:py-28"><div className="mx-auto max-w-7xl"><p className="section-label">About PPSU Events</p><h1 className="mt-5 max-w-4xl text-5xl leading-tight text-navy-950 md:text-8xl">The moments that make university feel like <span className="text-gold-500">yours.</span></h1><p className="mt-8 max-w-2xl text-lg leading-8 text-muted">PPSU Events is the home of the university’s most anticipated experiences — created to bring students together, celebrate every milestone, and make campus life unforgettable.</p></div></section><section className="bg-navy-950 px-6 py-20 text-white md:px-10 md:py-28"><div className="mx-auto grid max-w-7xl gap-12 md:grid-cols-3"><div className="md:col-span-2"><p className="section-label text-gold-200">Our point of view</p><h2 className="mt-5 max-w-3xl text-4xl md:text-6xl">A campus is more than its buildings. It is the stories we create together.</h2></div><p className="text-base leading-8 text-white/60">From intimate community gatherings to the flagship annual gala, every PPSU experience is designed with care, energy, and a deep belief in the power of belonging.</p></div></section><section className="px-6 py-20 md:px-10 md:py-28"><div className="mx-auto max-w-7xl"><TrustStrip /></div></section></div>; }
export function ContactPage() { return <div className="pt-28"><section className="px-6 py-20 md:px-10 md:py-28"><div className="mx-auto grid max-w-7xl gap-14 lg:grid-cols-2"><div><p className="section-label">Contact the team</p><h1 className="mt-5 text-5xl text-navy-950 md:text-7xl">Let’s make something worth remembering.</h1><p className="mt-7 max-w-md text-base leading-7 text-muted">Questions about an event, a booking, or partnering with PPSU Events? We would love to hear from you.</p><div className="mt-10 space-y-5 text-sm text-navy-900/70"><p><strong className="block text-navy-950">Email</strong>events@ppsu.ac.in</p><p><strong className="block text-navy-950">Visit</strong>P. P. Savani University, Surat, Gujarat</p></div></div><form onSubmit={(e) => { e.preventDefault(); }} className="rounded-[24px] bg-white p-7 shadow-xl shadow-navy-950/5 ring-1 ring-navy-950/5 md:p-10"><h2 className="font-display text-3xl text-navy-950">Send a note</h2><div className="mt-7 space-y-4"><input className="input-field" placeholder="Your name" required /><input className="input-field" type="email" placeholder="Your email" required /><select className="input-field"><option>General enquiry</option><option>Booking help</option><option>Partner with us</option></select><textarea className="input-field min-h-32" placeholder="How can we help?" required /><button className="btn-primary w-full">Send message <ArrowRight size={16} /></button></div></form></div></section></div>; }
